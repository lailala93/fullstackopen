```mermaid
flowchart TB
  A["User writes note and clicks Save"] -->|POST /exampleapp/new_note| B["Server saves note"]
  B -->|302 Redirect to /exampleapp/notes| C["Browser follows redirect (GET /exampleapp/notes)"]

  C --> D["HTML document"]
  C --> E["GET main.css"]
  C --> F["GET main.js"]
  C --> G["GET data.json (now includes new note)"]
